name: Compiler APK Borne Giga Controle
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'BorneGigaControle-source.zip'
      - '.github/workflows/borne-giga-apk.yml'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Extraire le projet
        run: |
          rm -rf /tmp/borne-giga-build
          mkdir -p /tmp/borne-giga-build
          unzip -q BorneGigaControle-source.zip -d /tmp/borne-giga-build
      - name: Compiler APK
        run: gradle -p /tmp/borne-giga-build/BorneGigaControle assembleDebug
      - name: Préparer APK
        run: cp /tmp/borne-giga-build/BorneGigaControle/app/build/outputs/apk/debug/app-debug.apk Borne-Giga-Controle.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Borne-Giga-Controle-APK
          path: Borne-Giga-Controle.apk
